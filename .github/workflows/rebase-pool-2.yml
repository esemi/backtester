name: rebase-pool-2

on:
  push:
    branches: [release-pool-2]

jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/rebase@v2
        with:
          base: master
          token: ${{ secrets.PAT }}
          include-labels: |
            rebase
