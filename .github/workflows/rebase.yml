name: rebase

on:
  push:
    branches: [master]

jobs:
  rebase:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/rebase@v2
        with:
          base: master
          include-labels: |
            rebase
